# مستند فنی سایت صبا رسانه (sabaseda.ir)

این فایل برای این نوشته شده که هر زمان — چه خود شما بعد از مدتی فراموش کردن جزئیات، چه یک نفر دیگه که قراره سایت رو تحویل بگیره — با خوندنش کامل بفهمه سایت از چی ساخته شده، هر ابزار برای چه کاری استفاده می‌شه، و کجا باید دنبال چی بگرده.

---

## ۱. معماری کلی، در یک نگاه

سایت **بدون دیتابیس سنتی** ساخته شده. به‌جاش:

- هر خبر یک **فایل متنی ساده (Markdown)** است که در **GitHub** ذخیره می‌شه.
- وقتی خبر جدیدی منتشر می‌کنید، **Cloudflare** خودکار سایت رو از نو «می‌سازه» (build) و منتشر می‌کنه.
- شما هیچ‌وقت مستقیم با کد کار نمی‌کنید — از یک **پنل مدیریت گرافیکی** (`/admin`) استفاده می‌کنید.

جریان کار به این شکله:

```
شما (پنل /admin) → GitHub (ذخیره‌ی فایل خبر) → Cloudflare (ساخت و انتشار خودکار سایت) → sabaseda.ir (سایت زنده)
```

---

## ۲. هر ابزار، برای چه کاری

| ابزار | نقشش در این پروژه | چرا انتخاب شد |
|---|---|---|
| **GitHub** | محل ذخیره‌ی تمام کدها، خبرها (فایل‌های Markdown)، و تصاویر سایت. هر تغییری این‌جا ذخیره (commit) می‌شه. | رایگان، نامحدود برای این حجم، و نسخه‌ی قبلی هر فایل همیشه قابل بازیابیه. |
| **Cloudflare (Workers)** | میزبان (هاست) واقعی سایت. هر بار که چیزی در GitHub تغییر کنه، Cloudflare خودکار سایت رو می‌سازه و منتشر می‌کنه. | رایگان، سریع، بدون محدودیت پهنای باند، و SSL/HTTPS خودکار. |
| **Astro** (فریم‌ورک) | زبان/ابزاری که کل ساختار، ظاهر، و منطق سایت باهاش نوشته شده (کدهای داخل پوشه‌ی `src`). | مخصوص سایت‌های محتوا-محور مثل خبر؛ خروجی بسیار سریع تولید می‌کنه. |
| **Sveltia CMS** (پنل `/admin`) | همون فرم گرافیکی که خبر می‌فرستید. مستقیم به GitHub وصله و پشت صحنه فایل Markdown می‌سازه. | رایگان، بدون نیاز به سرور جدا، مخصوص سایت‌های بدون دیتابیس. |
| **Cloudflare Web Analytics** | آمار بازدید سایت (چند نفر، چه صفحاتی، از کجا). | رایگان و بدون کوکی، برخلاف Google Analytics نیازی به بنر «قبول کوکی» نداره. |
| **Cloudflare KV + یک Worker مجزا (`saba-votes`)** | ذخیره‌ی شمارش لایک/دیس‌لایک هر خبر. | یک سرویس کاملاً جدا و ایزوله از سایت اصلی، تا هیچ خطری برای سایت نداشته باشه. |
| **GitHub Actions** (پوشه‌ی `.github/workflows`) | بعد از هر خبر جدید با عکس، خودکار عکس‌های سنگین رو فشرده می‌کنه. | رایگان، کاملاً خودکار، نیازی به کار دستی شما نداره. |
| **Google Search Console** | به گوگل می‌گه سایت شما وجود داره و باید ایندکس بشه؛ گزارش خطاهای فنی سئو رو هم می‌ده. | رایگان و استاندارد صنعتیه؛ بدونش عملاً گوگل شما رو دیر یا اصلاً پیدا نمی‌کنه. |
| **Google Publisher Center** | ثبت رسمی رسانه برای احتمال نمایش در Google News / Discover. | افزایش شانس دیده‌شدن خبرها در بخش‌های ویژه‌ی گوگل. |
| **فونت Vazirmatn** | فونت فارسی سراسر سایت. | معروف‌ترین و تمیزترین فونت متن‌باز فارسی، همونی که تلگرام دسکتاپ هم استفاده می‌کنه. |
| **دامنه sabaseda.ir** | ثبت‌شده جای دیگه (ریسلر/ثبت‌کننده‌ی دامنه، ظاهراً talahost.com)، ولی مدیریت DNS و اتصالش به Cloudflare منتقل شده. | — |

---

## ۳. کجا باید دنبال چی بگردید (نقشه‌ی حساب‌ها)

| برای این کار... | برید اینجا |
|---|---|
| ارسال/ویرایش/حذف خبر | `https://sabaseda.ir/admin` |
| دیدن این‌که سایت درست build شده یا نه | داشبورد Cloudflare → Workers & Pages → `sabaseda` → تب Deployments |
| دیدن آمار بازدید | داشبورد Cloudflare → Analytics & Logs → Web Analytics |
| دیدن یا ویرایش مستقیم کد/فایل‌ها | `https://github.com/shakibuser/sabaseda` |
| مدیریت دامنه (DNS، nameserver) | داشبورد Cloudflare → دامنه‌ی sabaseda.ir |
| خرید/تمدید اصل دامنه | همون ریسلری که دامنه رو ازش خریدید (talahost.com یا مشابه) |
| دیدن وضعیت ایندکس‌شدن در گوگل | Google Search Console (search.google.com/search-console) |
| ثبت رسمی برای Google News/Discover | Google Publisher Center (publishercenter.google.com) |
| سرویس لایک/دیس‌لایک (نادر لازمه بهش سر بزنید) | Worker جدا به اسم `saba-votes` در داشبورد Cloudflare |

---

## ۴. ساختار فایل‌های پروژه (برای کسی که می‌خواد کد رو نگاه کنه)

```
sabaseda/
├── src/
│   ├── components/       ← تکه‌های قابل‌استفاده‌ی مجدد (هدر، فوتر، کارت خبر، دکمه‌های اشتراک‌گذاری و...)
│   ├── content/
│   │   ├── news/         ← همه‌ی فایل‌های خبر (Markdown) — همونی که پنل ادمین می‌سازه
│   │   └── pages/         ← متن صفحه‌ی «درباره ما»
│   ├── layouts/           ← قالب پایه‌ی هر صفحه (تگ‌های head، فونت، آنالیتیکس)
│   ├── pages/              ← هر فایل اینجا یک صفحه یا API از سایته (خانه، آرشیو، دسته‌بندی، rss، sitemap و...)
│   ├── styles/             ← رنگ‌ها، فونت، استایل کلی سایت (global.css)
│   └── utils/              ← توابع کمکی (فرمت تاریخ فارسی، رنگ دسته‌بندی‌ها)
├── public/
│   ├── admin/              ← تنظیمات پنل مدیریت (config.yml) و صفحه‌ی ورودش
│   ├── images/              ← لوگو و عکس‌های خبر
│   └── _headers            ← تنظیمات امنیتی سایت (CSP و...)
├── .github/workflows/       ← فرآیند خودکار فشرده‌سازی عکس
├── scripts/                 ← اسکریپت فشرده‌سازی عکس
├── astro.config.mjs          ← تنظیمات اصلی Astro
└── package.json               ← لیست ابزارهای نرم‌افزاری مورد نیاز پروژه
```

---

## ۵. چطور خبر اضافه/ویرایش/حذف کنم؟

از پنل `/admin` استفاده کنید (نیازی به دونستن کد نیست). ورود با یک GitHub Personal Access Token انجام می‌شه که قبلاً ساختید. جزئیات کامل در `README.md` (پوشه‌ی اصلی پروژه در GitHub) موجوده.

---

## ۶. اگه یک روز خواستید سایت رو به کسی تحویل بدید

این‌ها رو باید بهش منتقل کنید:

1. دسترسی به ریپازیتوری GitHub (`shakibuser/sabaseda`) — یا Collaborator اضافه کنید، یا کل حساب رو منتقل کنید.
2. دسترسی به حساب Cloudflare (که سایت، دامنه، و Worker لایک/دیس‌لایک روشه).
3. اطلاعات دامنه (کجا خریداری شده، اطلاعات ورود به پنل ثبت‌کننده).
4. دسترسی به همون حساب گوگلی که Search Console و Publisher Center باهاش ثبت شده.
5. همین فایل مستند!

---

## ۷. یادداشت‌های مهم برای آینده

- **پلن رایگان Cloudflare** ظرفیت بسیار بالایی داره (صدها هزار بازدید در روز، ترافیک نامحدود) — برای سال‌ها کافیه.
- عکس‌های خبر خودکار فشرده می‌شن؛ نیازی به نگرانی درباره‌ی حجم نیست.
- تاریخچه‌ی کامل هر تغییری که تا امروز روی سایت انجام شده، در GitHub قابل مشاهده و بازگشت‌پذیره (چیزی هیچ‌وقت واقعاً از بین نمی‌ره).
- برای رفع سریع خطاهای رایج، جدول «راهنمای تشخیص خطا» در `README.md` رو ببینید.
